import ListeningPageClient from "@/components/ListeningPageClient";

export const metadata = {
  title: "Listening | Lakshya Paliwal",
  description: "What I've been listening to recently.",
};

export default function ListeningPage() {
  return <ListeningPageClient />;
}
